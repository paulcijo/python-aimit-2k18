// array demo in python
#include<stdio.h>
void main() {
    int vals[] = {4, 5, 6, 1, 2, 4, 2, 44, 5};
    int sum = 0;
    for (int i = 0; i < 9; i++)
    {
        sum += vals[i];
    }

    // deleting an element from the array
    // finding an element from an array
    // appending values to the array
    // sort the array
    // concatenate two arrays
    // slicing lists
}